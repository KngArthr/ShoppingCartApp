<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shoppingcart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FinalList"></activity>
        <activity android:name=".AddItem" />
        <activity android:name=".ItemsListScroll" />
        <activity
            android:name=".ScrollingItemList"
            android:label="@string/title_activity_scrolling_item_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ItemList"
            android:label="@string/title_activity_item_list"
            android:parentActivityName=".Central"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.shoppingcart.Central" />
        </activity>
        <activity android:name=".Central" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>